<% post = "Twistee" %>

</br></br></br><div class="small-12 medium-6 medium-centered columns">
  <div class="large-8 large-centered columns">
    <% @photos.reverse.each do |photo| %>
      <div class="photo card"  >
        <% if photo.name.include?("Twistee Treat") %>
          <%= image_tag photo.photo %>
        <% end %>
      </div></br>

    <% end %>
  </div>
</div>
